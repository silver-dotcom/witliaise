class Event {
  String title;
  // String desc;
  String addedBy;
  // String eventType;
  String date;
  // DateTime date;
  String startTime;
  String endTime;
  // TimeOfDay startTime;
  // TimeOfDay endTime;

  // Event(this.title, this.desc, this.addedBy, this.eventType, this.date,
  //     this.startTime, this.endTime);
  Event(this.title, this.addedBy, this.date, this.startTime, this.endTime);
}
